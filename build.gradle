buildscript {
    repositories {
        maven { url = 'https://maven.minecraftforge.net' }
        mavenCentral()
        maven { url = 'https://maven.parchmentmc.org' }
    }
    dependencies {
        classpath group: 'net.minecraftforge.gradle', name: 'ForgeGradle', version: '6.0.33+', changing: true
        classpath group: 'org.spongepowered', name: 'mixingradle', version: '0.7+'

        classpath group: 'org.parchmentmc', name: 'librarian', version: "1.+"
    }
}
plugins {
    id 'eclipse'
    id 'idea'
    id 'maven-publish'
    id 'net.minecraftforge.gradle' version '[6.0,6.2)'
}

apply plugin: 'net.minecraftforge.gradle'
apply plugin: 'org.spongepowered.mixin'
apply plugin: 'org.parchmentmc.librarian.forgegradle'

version = "${minecraft_min_version}-forge-${mod_version}"
group = "${mod_base_package}" // http://maven.apache.org/guides/mini/guide-naming-conventions.html
archivesBaseName = mod_name_s

// Mojang ships Java 17 to end users in 1.18+, so your mod should target Java 17.
java.toolchain.languageVersion = JavaLanguageVersion.of(17)

println "Java: ${System.getProperty 'java.version'}, JVM: ${System.getProperty 'java.vm.version'} (${System.getProperty 'java.vendor'}), Arch: ${System.getProperty 'os.arch'}"
//just for compatilily
sourceSets {
    onlyMain {
    }
}
minecraft {
    // The mappings can be changed at any time and must be in the following format.
    // Channel:   Version:
    // official   MCVersion             Official field/method names from Mojang mapping files
    // parchment  YYYY.MM.DD-MCVersion  Open community-sourced parameter names and javadocs layered on top of official
    //
    // You must be aware of the Mojang license when using the 'official' or 'parchment' mappings.
    // See more information here: https://github.com/MinecraftForge/MCPConfig/blob/master/Mojang.md
    //
    // Parchment is an unofficial project maintained by ParchmentMC, separate from MinecraftForge
    // Additional setup is needed to use their mappings: https://parchmentmc.org/docs/getting-started
    //
    // Use non-default mappings at your own risk. They may not always work.
    // Simply re-run your setup task after changing the mappings to update your workspace.
//    mappings channel: 'official', version: "${minecraft_min_version}"
    mappings channel: 'parchment', version: "${parchment_version}"
//    mappings channel: 'mcp', version: "20230612.114412"
    accessTransformer = file('src/main/resources/META-INF/accesstransformer.cfg')
    // This property allows configuring Gradle's ProcessResources task(s) to run on IDE output locations before launching the game.
    // It is REQUIRED to be set to true for this template to function.
    // See https://docs.gradle.org/current/dsl/org.gradle.language.jvm.tasks.ProcessResources.html
    // Default run configurations.
    // These can be tweaked, removed, or duplicated as needed.
    runs {
        client {
            workingDirectory project.file('run')

            // Recommended logging data for a userdev environment
            // The markers can be added/remove as needed separated by commas.
            // "SCAN": For mods scan.
            // "REGISTRIES": For firing of registry events.
            // "REGISTRYDUMP": For getting the contents of all registries.
            property 'forge.logging.markers', 'REGISTRIES'

            // Recommended logging level for the console
            // You can set various levels here.
            // Please read: https://stackoverflow.com/questions/2031163/when-to-use-the-different-log-levels
            property 'forge.logging.console.level', 'debug'

            // Comma-separated list of namespaces to load gametests from. Empty = all namespaces.
            property 'forge.enabledGameTestNamespaces', mod_id

            property 'mixin.env.remapRefMap', 'true'
            property 'mixin.env.refMapRemappingFile', "${buildDir}/createSrgToMcp/output.srg"

            // use for logger control
            property 'forgegradle.runs.dev', 'true'

            jvmArgs "-Xmx2048M"
            mods {
                "${mod_id}" {
                    source sourceSets.main
                    source sourceSets.onlyMain
                }
            }
        }

        server {
            workingDirectory project.file('run')

            property 'forge.logging.markers', 'REGISTRIES'

            property 'forge.logging.console.level', 'debug'

            property 'forge.enabledGameTestNamespaces', mod_id

            property 'mixin.env.remapRefMap', 'true'
            property 'mixin.env.refMapRemappingFile', "${buildDir}/createSrgToMcp/output.srg"

            // use for logger control
            property 'forgegradle.runs.dev', 'true'
            property 'forgegradle.runs.runServer', 'true'

            mods {
                "${mod_id}" {
                    source sourceSets.main
                }
            }
        }

        // This run config launches GameTestServer and runs all registered gametests, then exits.
        // By default, the server will crash when no gametests are provided.
        // The gametest system is also enabled by default for other run configs under the /test command.
        gameTestServer {
            workingDirectory project.file('run')

            property 'forge.logging.markers', 'REGISTRIES'

            property 'forge.logging.console.level', 'debug'

            property 'forge.enabledGameTestNamespaces', mod_id

            property 'mixin.env.remapRefMap', 'true'
            property 'mixin.env.refMapRemappingFile', "${buildDir}/createSrgToMcp/output.srg"

            mods {
                "${mod_id}" {
                    source sourceSets.main
                }
            }
        }

        data {
            workingDirectory project.file('run')

            property 'forge.logging.markers', 'REGISTRIES'

            property 'forge.logging.console.level', 'debug'

            // Specify the modid for data generation, where to output the resulting resource, and where to look for existing resources.
            args '--mod', mod_id, '--all', '--output', file('src/generated/resources/'), '--existing', file('src/main/resources/')

            property 'mixin.env.remapRefMap', 'true'
            property 'mixin.env.refMapRemappingFile', "${buildDir}/createSrgToMcp/output.srg"
            // add runData info
            property 'forgegradle.runs.runData', 'true'
            property 'forgegradle.runs.dev', 'true'


            property 'eclipticseasons.runs.runData', 'true'

            mods {
                "${mod_id}" {
                    source sourceSets.main
                }
            }
        }
    }
}


tasks.named('processResources', Copy) {
    duplicatesStrategy = DuplicatesStrategy.INCLUDE

    doFirst {
        def langDir = file("src/main/resources/assets/${mod_id}/lang")
        def genDir = file("src/generated/resources/assets/${mod_id}/lang")

        def langFiles = ['en_us.json', 'zh_cn.json']

        project.ext.originalJsonMap = [:]

        langFiles.each { fileName ->
            def mainFile = new File(langDir, fileName)
            def genFile = new File(genDir, fileName)

            project.ext.originalJsonMap[fileName] = genFile.exists() ? genFile.text : null

            def mergedMap = [:]
            [mainFile, genFile].each { f ->
                if (f.exists()) mergedMap.putAll(new groovy.json.JsonSlurper().parse(f))
            }

            if (genFile.exists()) {
                genFile.text = groovy.json.JsonOutput.prettyPrint(groovy.json.JsonOutput.toJson(mergedMap))
            }
        }
    }

    doLast {
        def genDir = file("src/generated/resources/assets/${mod_id}/lang")
        def langFiles = ['en_us.json', 'zh_cn.json']

        langFiles.each { fileName ->
            def genFile = new File(genDir, fileName)
            if (project.ext.originalJsonMap[fileName] != null) {
                genFile.text = project.ext.originalJsonMap[fileName]
            }
        }
    }
}

mixin {
    add sourceSets.main, "${mod_id}.refmap.json"
    // Sets up the mixin config; this gets added to run configurations and the manifest in the final jar
    config "${mod_id}.mixins.json"

    // Enables exporting mixin-changed classes to .mixin.out in the run folder
    debug.export = true
}

// Include resources generated by data generators.
sourceSets.main.resources { srcDir 'src/generated/resources' }

repositories {
    // Put repositories for dependencies here
    // ForgeGradle automatically adds the Forge maven and Maven Central for you

    // If you have mod jar dependencies in ./libs, you can declare them as a repository like so:
    flatDir {
        dir 'libs'
        content {
            includeGroup "libs"
        }
    }

    flatDir {
        dir 'mods'
        content {
            includeGroup "mods"
        }
    }

    flatDir {
        dir 'mods_server'
        content {
            includeGroup "mods_server"
        }
    }

    maven {
        url "https://cursemaven.com"
        content {
            includeGroup "curse.maven"
        }
    }
//    maven {
//        url "https://maven.su5ed.dev/releases"
//    }
    maven {
        url "https://harleyoconnor.com/maven"
        content {
            includeGroup "com.ferreusveritas.dynamictrees"
        }
    }
}

dependencies {
    // Specify the version of Minecraft to use. If this is any group other than 'net.minecraft', it is assumed
    // that the dep is a ForgeGradle 'patcher' dependency, and its patches will be applied.
    // The userdev artifact is a special name and will get all sorts of transformations applied to it.
    minecraft "net.minecraftforge:forge:${minecraft_min_version}-${forge_version}"
    annotationProcessor 'org.spongepowered:mixin:0.8.5:processor'

    compileOnly(annotationProcessor("io.github.llamalad7:mixinextras-common:0.4.1"))
    implementation(jarJar("io.github.llamalad7:mixinextras-forge:0.4.1")) {
        jarJar.ranged(it, "[0.4.1,)")
    }
    fg.deobf("curse.maven:configured-457570:5180900")

    implementation fg.deobf("curse.maven:jei-238222:6075247")
    implementation fg.deobf("curse.maven:jade-324717:5072729")
    compileOnly fg.deobf("curse.maven:the-one-probe-245211:6106996")

    implementation fg.deobf("curse.maven:patchouli-306770:6164575")

    compileOnly fg.deobf("curse.maven:kubejs-238086:5853326")

    compileOnly fg.deobf("libs:fabric-renderer-api-v1:3.2.1+cf68abbe77")
    compileOnly fg.deobf("libs:fabric-renderer-indigo:1.5.2+b5b2da4177")

//    compileOnly fg.deobf("libs:voxy:0.2.7-alpha")
//    compileOnly fg.deobf("libs:BigGlobe:5.2.0-MC1.20.1")

    compileOnly fg.deobf("curse.maven:embeddium-908741:5681725")
    compileOnly fg.deobf("curse.maven:oculus-581495:6020952")

    onlyMainRuntimeOnly fg.deobf("curse.maven:embeddium-908741:5681725")
    onlyMainRuntimeOnly fg.deobf("curse.maven:oculus-581495:6020952")

//    implementation fg.deobf("curse.maven:kiwi-303657:4688568")
//    implementation fg.deobf("curse.maven:snow-real-magic-308663:5139853")

    onlyMainRuntimeOnly fg.deobf("curse.maven:journeymap-32274:4873863")
    onlyMainRuntimeOnly fg.deobf("curse.maven:spark-361579:4587309")
    // Real mod deobf dependency examples - these get remapped to your current mappings
    // implementation fg.deobf("com.tterrag.registrate:Registrate:MC${mc_version}-${registrate_version}") // Adds registrate as a dependency

    compileOnly fg.deobf("curse.maven:farmers-delight-398521:5051242")

    compileOnly fg.deobf("curse.maven:distant-horizons-508933:7097100")
//    onlyMainRuntimeOnly fg.deobf("curse.maven:distant-horizons-508933:7097100")

    compileOnly fg.deobf("curse.maven:improperui-forged-1209983:6362682")

    compileOnly fg.deobf("curse.maven:modern-ui-352491:6956345")
//    onlyMainRuntimeOnly fg.deobf("curse.maven:modern-ui-352491:6956345")
//    onlyMainRuntimeOnly fg.deobf("curse.maven:cloth-config-348521:5729105")
//    onlyMainRuntimeOnly fg.deobf("curse.maven:architectury-api-419699:5137938")
//    onlyMainRuntimeOnly fg.deobf("curse.maven:stevesrealisticsleep-616330:5962707")
//    onlyMainRuntimeOnly fg.deobf("curse.maven:distant-horizons-508933:6791186")

//    compileOnly fg.deobf("curse.maven:legendary-survival-overhaul-840254:6703282")

//    onlyMainRuntimeOnly fg.deobf("curse.maven:snow-under-trees-353942:6751711")

//    onlyMainRuntimeOnly  fg.deobf("curse.maven:hourglass-511172:4834578")

//    onlyMainRuntimeOnly fg.deobf("curse.maven:configured-457570:5096828")
    // Examples using mod jars from ./libs
//    compileOnly "libs:OptiFine:1.20.1_HD_U_I6"
//    compileOnly project(':sub')

    // https://mvnrepository.com/artifact/net.time4j/time4j-base
    compileOnly("net.time4j:time4j-base:5.9.4")
//    implementation(jarJar("net.time4j:time4j-base:5.9.4")) {
//        jarJar.ranged(it, "[5.9.4,)")
//    }

    compileOnly project(':sub')

    println("----Scan onlymain libs begin----")
    fileTree(dir: 'mods', include: ['*.jar']).each { File file ->
        def fileName = file.name
        println(fileName.substring(0, fileName.indexOf("-")) + "," + fileName.substring(fileName.indexOf("-") + 1, fileName.lastIndexOf(".jar")))
        onlyMainRuntimeOnly fg.deobf(name: fileName.substring(0, fileName.lastIndexOf(".jar")),
                group: 'mods',
                version: fileName.substring(0, fileName.lastIndexOf(".jar")))
    }
    println("----Scan onlymain libs end----")

    println("----Scan onlymain server mods begin----")
    fileTree(dir: 'mods_server', include: ['*.jar']).each { File file ->
        def fileName = file.name
        println(fileName.substring(0, fileName.indexOf("-")) + "," + fileName.substring(fileName.indexOf("-") + 1, fileName.lastIndexOf(".jar")))
        runtimeOnly fg.deobf(name: fileName.substring(0, fileName.lastIndexOf(".jar")),
                group: 'mods_server',
                version: fileName.substring(0, fileName.lastIndexOf(".jar")))
    }
    println("----Scan onlymain server mods end----")

    // others
    // https://mvnrepository.com/artifact/org.projectlombok/lombok
    compileOnly 'org.projectlombok:lombok:1.18.38'
    annotationProcessor 'org.projectlombok:lombok:1.18.38'
    // For more info...
    // http://www.gradle.org/docs/current/userguide/artifact_dependencies_tutorial.html
    // http://www.gradle.org/docs/current/userguide/dependency_management.html
}

// Example for how to get properties into the manifest for reading at runtime.
jar {
    manifest {
        attributes([
                "Specification-Title"     : mod_id,
                "Specification-Vendor"    : mod_authors,
                "Specification-Version"   : "1", // We are version 1 of ourselves
                "Implementation-Title"    : project.name,
                "Implementation-Version"  : project.jar.archiveVersion,
                "Implementation-Vendor"   : mod_authors,
                "Implementation-Timestamp": new Date().format("yyyy-MM-dd'T'HH:mm:ssZ")
        ])
    }
}

// This block of code expands all declared replace properties in the specified resource targets.
// A missing property will result in an error. Properties are expanded using ${} Groovy notation.
// When "copyIdeResources" is enabled, this will also run before the game launches in IDE environments.
// See https://docs.gradle.org/current/dsl/org.gradle.language.jvm.tasks.ProcessResources.html
tasks.named('processResources', ProcessResources).configure {
    var replaceProperties = [
            minecraft_version      : minecraft_version,
            minecraft_version_range: minecraft_version_range,
            forge_version          : forge_version,
            forge_version_range    : forge_version_range,
            loader_version_range   : loader_version_range,
            mod_id                 : mod_id,
            mod_name               : mod_name,
            mod_license            : mod_license,
            mod_version            : mod_version,
            mod_authors            : mod_authors,
            mod_description        : mod_description,
            mod_update_json        : mod_update_json,
            mod_url                : mod_url,
    ]
    inputs.properties replaceProperties

    filesMatching(['META-INF/mods.toml', 'pack.mcmeta']) {
        expand replaceProperties + [project: project]
    }
}

tasks.register('deleteCache') {
    delete 'src/generated/resources/.cache'
}

jar.dependsOn deleteCache

// Example configuration to allow publishing using the maven-publish plugin
// This is the preferred method to reobfuscate your jar file
jar.finalizedBy('reobfJar')
// However if you are in a multi-project build, dev time needs unobfed jar files, so you can delay the obfuscation until publishing by doing
// publish.dependsOn('reobfJar')

publishing {
    publications {
        mavenJava(MavenPublication) {
            artifact jar
        }
    }
    repositories {
        maven {
            url "file://${project.projectDir}/mcmodsrepo"
        }
    }
}

tasks.withType(JavaCompile).configureEach {
    options.encoding = 'UTF-8' // Use the UTF-8 charset for Java compilation
}
